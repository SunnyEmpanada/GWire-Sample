# Replace this workflow with your registry deploy (e.g. ECR, GHCR, Fly.io).
# Previously this repo dispatched to Kognitos downstream repos.

name: Publish

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Add your deploy steps here (e.g. docker push)."
